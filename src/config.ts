/** Name painted on the fascia and shown on the loader / flat version. */
export const CAFE_NAME = "Jake's Cafe";

/** Neon tube colour used by the "say g'day" and "open" signs. */
export const NEON_COLOR = '#ff9d5c';
